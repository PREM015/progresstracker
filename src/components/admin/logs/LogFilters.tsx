'use client';
export function LogFilters({ filters, onChange }: any) { return <div className="grid grid-cols-3 gap-4"><select value={filters.level} onChange={e => onChange({ ...filters, level: e.target.value })} className="px-4 py-2 bg-zinc-900 border border-zinc-800 rounded-lg text-white"><option value="">All Levels</option><option value="ERROR">Error</option><option value="WARN">Warning</option><option value="INFO">Info</option></select><input type="date" value={filters.startDate} onChange={e => onChange({ ...filters, startDate: e.target.value })} className="px-4 py-2 bg-zinc-900 border border-zinc-800 rounded-lg text-white" /><input type="text" placeholder="Search..." value={filters.search} onChange={e => onChange({ ...filters, search: e.target.value })} className="px-4 py-2 bg-zinc-900 border border-zinc-800 rounded-lg text-white" /></div>; }
export default LogFilters;
